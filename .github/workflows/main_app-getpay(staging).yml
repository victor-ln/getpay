name: Deploy PHP App to Azure Web App/staging

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      AZURE_WEBAPP_APP_NAME: 'app-getpay'
      AZURE_WEBAPP_SLOT_NAME: 'staging'

    steps:
    # Passo 1: Clonar o repositório
    - name: Checkout code
      uses: actions/checkout@v4

    # Passo 2: Configurar o ambiente PHP com cache
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        # MUDANÇA PRINCIPAL: Trocado pdo_mysql por pdo_pgsql para alinhar com o ambiente de produção
        extensions: mbstring, xml, ctype, json, curl, dom, fileinfo, pdo, pdo_pgsql
        tools: composer
        coverage: none # Desabilitar Xdebug para performance
        cache: 'composer'

    # Passo 3: Instalar dependências do Composer (apenas para produção)
    - name: Install Composer dependencies
      run: composer install --no-progress --no-suggest --prefer-dist --optimize-autoloader --no-dev

    # Passo 4: Preparar o arquivo .env
    - name: Prepare .env file
      run: cp .env.example .env

    # Passo 5: Gerar a chave da aplicação Laravel
    - name: Generate Laravel Application Key
      run: php artisan key:generate

    # Passo 6: Configurar o ambiente Node.js
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    # Passo 7: Instalar dependências do NPM
    - name: Install NPM dependencies
      run: npm install

    # Passo 8: Compilar os assets (CSS/JS)
    - name: Build with NPM
      run: npm run build

    # Passo 9: Remover a pasta node_modules
    - name: Remove node_modules
      run: rm -rf node_modules

    # Passo 10: Fazer o deploy para o Azure Web App
    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_APP_NAME }}
        slot-name: ${{ env.AZURE_WEBAPP_SLOT_NAME }}
        publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_58CEDE20349640D6921683CEBAC3DEFB }}
        package: .
